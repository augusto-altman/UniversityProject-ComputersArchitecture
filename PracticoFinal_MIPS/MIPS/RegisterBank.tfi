


// Instantiate the module
RegisterBank instance_name (
    .clock(clock), 
    .addr1(addr1), 
    .addr2(addr2), 
    .writeAddr(writeAddr), 
    .writeData(writeData), 
    .regWrite(regWrite), 
    .reg1(reg1), 
    .reg2(reg2)
    );


