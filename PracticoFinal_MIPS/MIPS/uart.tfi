


// Instantiate the module
uart instance_name (
    .clk(clk), 
    .reset(reset), 
    .rx(rx), 
    .rd(rd), 
    .wr(wr), 
    .rd_empty(rd_empty), 
    .wr_full(wr_full), 
    .w_data(w_data), 
    .tx(tx), 
    .r_data(r_data)
    );


