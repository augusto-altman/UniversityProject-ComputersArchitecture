


// Instantiate the module
u_data_memory instance_name (
    .clock(clock), 
    .read(read), 
    .write(write), 
    .address(address), 
    .in_data(in_data), 
    .out_data(out_data)
    );


